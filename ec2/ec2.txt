
EC2 : Elastic Compute Cloud : 

Server : Virtual Machiene : EC2 (AWS) : Azure VM (Azure) : GCP (Compute Engine)

Basically a server is a powerful computer that serves information, files, or services to other computers. How a server works:
1. The request: You click a link, check your email, or open an application.
2. The delivery: Your device sends a digital request over the internet to the server.
3. The response: The server processes a request then it sends the response back to your device.


The commonly used servers are:
- web servers
- game servers
- file servers
- mail servers
- Database Server
- Caching Server

Why can't we use our laptop/low hoardware as a server?
1. Servers never sleep. They are designed to run 24 hours a day, 365 days a year without crashing.
2. They are built for huge traffic handling. A laptop can handle one user or ten users but a server handles thousands of users at the exact same time.

------------

--> pay-as-you-go : Pay for the resource you actually use.
--> pay-as-you-grow : Pay for the resources usage basis.. (more customers --> more servers --> more price)
--> Go global in minutes

------------

1. On-Demand Instances : 
--> No commitmenet with AWS - pay-as-you-go.
--> Dev/IT/Test, Unptedictable workload, New application testings.
--> Most expensive per hour.
--> AWS cost us per seconds with min 60 sec.

---------

2. Reserved Instance : 
--> Commitment for 1yr / 3 yrs.. You get big discount in the pricing.
--> This is best for a steady predictable workload or an always-on kind of application. 

Reserved instances we have two options:
1. Standard reserved instance: This will be locked to a specific instance type or region with a good discount.
2. Convertible reserved instance: We can change the instance family/size at a slightly lower discount than a standard reserved instance but we have that flexibility to change the configuration.

Three pricing options in reserved instances:
1. Full upfront: pay 100% of the amount and you get your instance.
2. Partial upfront: pay 30-60% of the amount now then AWS will reduce the hourly price, you pay based on the reduced hourly price.
3. No upfront: pay everything on a usage basis.

** We can sell our reserved instances in AWS Marketplace, if we dont want it anymore. 

---------

3. Savings Plan : Savings Plans are a flexible pricing model that offer low prices on AWS usage, in exchange for a commitment to a consistent amount of usage (measured in $/hour) for a 1- or 3-year term.

Compute Savings Plans : Applies to EC2 instance usage, AWS Fargate, and AWS Lambda service usage, regardless of Region, instance family, size, tenancy, and operating system.

EC2 Instance Savings Plans : Applies to instance usage within the committed EC2 family and region, regardless of size, tenancy, and operating system.

Database Savings Plans : Applies to Aurora, RDS, DynamoDB, ElastiCache, Amazon DocumentDB, Neptune, Amazon OpenSearch Service, and other services regardless of supported engine, instance family, size, deployment option, or Region.

SageMaker Savings Plans : Applies to SageMaker service usage, regardless of Region, instance family, and component.

---------

4. Spot instances
--> Bid for spare capacity. If our price is matching with aws spot price, aws provides an ec2 instance.
--> These instance can be interrupted at any time, problem is: 2 minutes warning period. 
--> If our application can be interrupted, even if any interruptions happen, no effect on our business processes 

--> If price changed, AWS terminated the instance, we no need to pay for partial hours.
--> If price not changed, YOU terminated instance, You need to pay for full duration.

2 hrs 55 Minutes ==> 
AWS terminate: 2 Hrs
YOU terminate: 2 hrs 55 Minutes

===========================================

D: 17/08/2026

General Purpose : balanced CPU + memory + network performance = T / M
Compute Optimised : More CPU powered Instancea categery : C
Memory Otimised : works load that req more data storage in RAM : R, U, X, Z
Storage optimised : High IOPS / Throughput : I, D, H
Accelerated Computing : CPU Installed and supported hardware : P, G, Trn
HPC : High performance compute : hpc

IOPS : Input and Outpur operations per second.

i --> intel Processor
a --> amd processor
g --> graviton processor

========

Common Naming strategies we use in real environments

P-LNX-WEB-ICI-03

[Environment]-[Platform]-[ServiceType]-[clientID]-[S.No]

Environment: 
P --> Production
U --> uat
Q --> QA 
D --> Dev
T --> Training
S --> sandbox
L --> Central Logging
N --> Central Networking

Platform: 
windows : WIN
linux : LNX

Service Type: 
APP : application server
DBS : database server
Web : web server
FSR : file server
MEX : exchange server
HDP : hadoop distro

Client / Project id: 
INT : Internal
YES : yes bank
ICI : icici bank
HDF : hdfc bank

---


Step 1 : Name and tags

Tag used to add metadata for our instance. 

Name = above standard
project = Project X
Client id = client id
Cost Center = AAZAA
Platform = Linux / Windows
owner/point of contact = Avinash


Step 2 : Application and OS Images (Amazon Machine Image) - Choose AMI
Choosing an AMI is nothing but selecting a preinstalled and configured operating system image to run along with the hardware. 

Step 3 : Choose Instance type

t3.micro
t3.small
c7i-flex.large		--> Choose This
m7i-flex.large

Step 4 : choose keypair

An AWS key pair is a security credential mechanism used to authenticate your identity when connecting to an EC2 instance. It uses an asymmetric encryption method, meaning a public key and private key method. 

AWS holds the public key and we will have our private key. When we want to get connected to the instance, we need to browse for the private key. The public key will be validated and verified against the private key we shared. If it matches, it allows us to log in or it generates a password. 

Public Key ==> With AWS		==> Inside ec2 instance (~/.ssh/authorized_keys)
Private key ==> With us

	  laptop					AWS Cloud/Instance
	Private key	------------------>	Public Key			==> Matching, pwd generates


Step 5 : network COnfiguration

Firewall (security groups) : A security group is a set of firewall rules that control the traffic for your instance. Add rules to allow specific traffic to reach your instance.

ports: 65536 : 0 - 65535

Windows connect : RDP : Remote Desktop Protocol : 3389
Linux connect : SSH : Secure shell : 22
web : http : 80
secure web : https : 443
DNS : 53
ftp : file transfer protocol : 20 & 21
NFS : Network File System : 2049
mysql : 3306
mssql : 1433
postgresql: 5432


From where you want to connect to instance.

1. anywhere : 0.0.0.0/0 :::/0
2. Custom : we can provide custom network IP addresses here. 
3. myIP : This picks your currently connected network IP address. 

Step 6 : Configure storage (EBS)

Windows: 30 gb
Linux: 8 gb / 10 gb

=================

To connect to Windows instances we need to have the supported tool:
1. If you are using a Mac operating system, you download the Windows app from the App Store.

2. Windows OS : 
	search --> "remote Desktop" --> open app and provide instance public ip, Username, password.. popup yes
	windows+r --> type "mstsc" --> click enter --> instance public ip, Username, password.. popup yes

















